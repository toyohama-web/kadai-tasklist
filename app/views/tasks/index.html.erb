<!--タスク一覧ページ -->
<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= task.id %>：<%= link_to task.content, task %>　進行状況：<%= task.status %></li>
  <% end %>
</ul>

<%= link_to "新規タスクの登録", new_task_path %>